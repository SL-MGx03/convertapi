services:
  - type: web
    name: convertapi
    env: docker
    plan: free
    autoDeploy: true
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    numInstances: 1
    region: oregon
    envVars:
      - key: NODE_ENV
        value: production
